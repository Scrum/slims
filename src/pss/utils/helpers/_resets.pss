@each $position in top, bottom, left, right {
  .reset-offset-$(position) {
    margin-$(position): initial;
  }
  .reset-offset-inside-$(position) {
    padding-$(position): initial;
  }
  .reset-box-$(position) {
    $(position): initial;
  }
}

@each $position in v, h {
  @if ($position == 'v') {
    .reset-offset-$(position) {
      margin-left: initial;
      margin-right: initial;
    }
    .reset-offset-inside-$(position) {
      padding-left: initial;
      padding-right: initial;
    }
  }

  @if ($position == 'h') {
    .reset-offset-$(position) {
      margin-top: initial;
      margin-bottom: initial;
    }
    .reset-offset-inside-$(position) {
      padding-top: initial;
      padding-bottom: initial;
    }
  }
}

.reset-col {
  width: initial;
}
